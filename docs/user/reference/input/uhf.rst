UHF Parameters
==============

Parameters specific to the UHF solver (``uhf_dry.out``).  For parameters
shared across solvers see :doc:`common`.

The UHF solver requires ``nelec`` and accepts ``2Sz``; see :doc:`common` for
their behaviour.  No additional UHF-specific keywords are currently exposed
through the StdFace input format.
