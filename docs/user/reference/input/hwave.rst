H-wave Parameters
=================

Parameters specific to the H-wave solver (``hwave_dry.out``).  For parameters
shared across solvers see :doc:`common`.

The H-wave solver requires ``nelec`` and accepts ``2Sz``; see :doc:`common`
for their behaviour.  No additional H-wave-specific keywords are currently
exposed through the StdFace input format.
