set(TEST_MODE "python" CACHE STRING "Integration test mode: python (stdface CLI) or base (C dry binaries)")

if (HPHI)
    add_subdirectory(hphi)
endif()

if (MVMC)
    add_subdirectory(mvmc)
endif()

if (UHF)
    add_subdirectory(uhf)
endif()

if (HWAVE)
    add_subdirectory(hwave)
endif()
